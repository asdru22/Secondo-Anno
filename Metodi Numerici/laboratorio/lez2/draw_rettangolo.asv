function draw_rettangolo[out]= (v);
min_x = min(v(:,1));
min_y = min(v(:,2));
max_x = max(v(:,1));
max_y = max(v(:,2));
out = [
    [min_x,min_y];
    [max_x,min_y];
    [max_x,max_y];
    [min_x,max_y];
    [min_x,min_y]
    ];
end